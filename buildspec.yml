version: 0.1

phases:
  build:
    commands:
    - sudo su -
    - npm install
    - npm run Webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
    - dist/bundle.js
